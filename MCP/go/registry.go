package main

import (
	"github.com/webhook-example/mcp-server/config"
	"github.com/webhook-example/mcp-server/models"
)

func GetAll(cfg *config.APIConfig) []models.Tool {
	return []models.Tool{
	}
}
